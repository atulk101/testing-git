let addNumber = function(a = number, b = number) {
  if (!isNaN(a) && !isNaN(b)) return a + b;
  return NaN;
};

let subtractNumber = function() {
  if (!isNaN(a) && !isNaN(b)) return a - b;
  return NaN;
};
